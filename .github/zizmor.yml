rules:
  cache-poisoning:
    ignore:
      # The release validation job is read-only, and the privileged publish job
      # does not consume artifacts, caches, or node_modules from validation.
      - release.yml:56
